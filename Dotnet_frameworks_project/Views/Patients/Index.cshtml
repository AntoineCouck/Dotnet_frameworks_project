@model IEnumerable<Dotnet_frameworks_project.Models.Patient>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<form asp-controller="Patients" asp-action="Index" method="get">
        <small>Search Name or lastname </small> <input type="text" name="nameFilter"/>
         
   @*        <div class="input-field col s12">
    <select  name="genderfilter" asp-items="@ViewBag.genderId">
      <option value="" disabled selected>Choose your option</option>
      <option value="">All</option>
    </select>
    
  </div>*@
        
  <button class="waves-effect waves-light btn-small" type="submit" name="action">Submit
    
  </button>
    </form>


    

<table class="table">
    <thead>
        <tr>
            <th>
                   <a asp-action="Index" asp-route-orderBy="@ViewBag.FirstName">@Html.DisplayNameFor(model => model.FirstName)</a>
            </th>
            <th>
                  <a asp-action="Index" asp-route-orderBy="@ViewBag.LastName">@Html.DisplayNameFor(model => model.LastName)</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-orderBy="@ViewBag.BirthDay">@Html.DisplayNameFor(model => model.Birthday)</a>
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ParentsPhone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LeftSessions)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.user)
            </th>
               <th>
                @Html.DisplayNameFor(model => model.Insurance)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Birthday)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParentsPhone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LeftSessions)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.user.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Insurance.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<p>
   
<a class="btn-floating btn-large waves-effect waves-light red"  asp-action="Create"><i class="material-icons">add</i></a>
</p>

